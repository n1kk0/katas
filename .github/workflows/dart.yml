name: Dart

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image:  google/dart:latest

    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: |
        cd dart
        pub get
        pub run test
        pub run test_coverage
