name: PHP Composer

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Run test suite
      run: |
        cd php
        composer install
        vendor/bin/phpunit tests
